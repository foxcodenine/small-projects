OK 3. add swap  functionalty to project images

OK 4. add border to cover image

OK 5. Delete Project remove all image from aws

OK 6. Add hosted field to projects

sudo apt-get install php8.0-gd
sudo apt install php8.0-xmlwriter
sudo systemctl restart apache2

remove outerProjectImages JS bug

start working on dashboard

4. Create dummy data
    Clients
    Project
    Images


F. Add add json inteface to project  and check client project images json

G. Use MyUtilities::optionInDB method 
   in Clients and remove redundant methods from MyUtilities


------------------------------------------------------------------------
Y. demo delete time
X. create a Deploy folder
    include vitual host

------------------------------------------------------------------------


3. Update message colors to match that of the signing image
4. Add time and date to email body
5. update email body


------------------------------------------------------------------------

header('Content-Type: application/json');