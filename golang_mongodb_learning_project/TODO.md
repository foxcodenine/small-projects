# TODO

## Completed

- Complete the small learning project `golang-mongodb`.
- Build a small Go API with MongoDB for a `users` collection.
- Implement basic CRUD operations for users:
  - create user
  - get all users
  - get user by id
  - update user by id
  - delete user by id
- Add password hashing for user creation and update.
- Add basic request validation and JSON error responses.
- Add simple handler tests for `internal/api`.
- Practice Go project structure with:
  - handlers
  - repository layer
  - models
  - router

## Maybe Later

- Add JWT authentication.
- Validate protected API calls with middleware.
- Add user login logs / audit logs.
- Add an API key generator.
- Add more tests, especially success-path handler tests.
- Add repository tests or integration tests with MongoDB.
- Improve coverage after learning more testing basics.

## Next Learning Step

- Start `Learn Go with Tests`.
- Come back later and improve this project with better testing and design patterns.
